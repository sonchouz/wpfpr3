<Page x:Class="wpfpr3.Pages.Employer"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:wpfpr3.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Employer" Height="450" Width="800" Background="White">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="220*" />
            <ColumnDefinition Width="260*" />
            <ColumnDefinition Width="260*" />
            <ColumnDefinition Width="260*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="25*" />
            <RowDefinition Height="5*" />
            <RowDefinition Height="75*" />
            <RowDefinition Height="25*" />
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Grid.Row="0" Grid.Column="0"
Grid.ColumnSpan="3" HorizontalAlignment="Right">
            <TextBlock Text="Поиск по фамилии: " />
        </StackPanel>
        <TextBox Name="txtFullName" Grid.Row="1" Grid.Column="0"
SelectionChanged="txtFullName_SelectionChanged" VerticalAlignment="Center"
Margin="0, 0, 5, 0" TextChanged="txtFullName_TextChanged" />
        <ComboBox Name="cmbSorting" Grid.Row="1" Grid.Column="1"
ItemsSource="{Binding SortingList}"
SelectionChanged="cmbSorting_SelectionChanged" SelectedIndex="0"
VerticalAlignment="Center" Margin="5, 0, 5, 0" />
        <ComboBox Name="cmbFilter" Grid.Row="1" Grid.Column="2"
ItemsSource="{Binding FilterList}" SelectionChanged="cmbFilter_SelectionChanged"
SelectedIndex="0" Margin="5, 0, 5, 0" VerticalAlignment="Center" />
        <StackPanel Orientation="Horizontal" Grid.Row="1" Grid.Column="3"
VerticalAlignment="Center" HorizontalAlignment="Center">
            <TextBlock x:Name="txtResultCount" />
            <TextBlock Text="из" />
            <TextBlock x:Name="txtAllAmount"/>
        </StackPanel>
        <ListView Name="lViewCand" Grid.Column="0" Grid.ColumnSpan="4"
Grid.Row="2" Margin="5,5,5,5" d:ItemsSource="{d:SampleData ItemCount=1}"
MouseDoubleClick="lViewCand_MouseDoubleClick">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment"
Value="Stretch" />
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="260*" />
                                <ColumnDefinition Width="260*" />
                                <ColumnDefinition Width="260*" />
                                <ColumnDefinition Width="260*" />
                            </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <StackPanel Grid.Column="1" Margin="5">
                                <TextBlock Text="{Binding User.firstname}"
FontWeight="Bold" />
                                <TextBlock Text="{Binding
User.surname}" />
                                <TextBlock Text="{Binding User.roleID}" />
                            </StackPanel>
                            <StackPanel Orientation="Vertical" Grid.Column="2"
VerticalAlignment="Center">
                                <TextBlock Text="{Binding User.birthday}" />
                                <TextBlock Text="{Binding User.email, StringFormat={}(0)}" />
                                <TextBlock Text="{Binding citizenship}" />
                            </StackPanel>
                            <StackPanel Grid.Column="3"
VerticalAlignment="Center">
                                <TextBlock Grid.Column="2" Text="{Binding
Education.edulevel, StringFormat={}(0) %}" VerticalAlignment="Center" />
                                <TextBlock Text="{Binding livingcity}" />
                                <TextBlock Text="{Binding User.phone}" />
                            </StackPanel>
                        </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
            <ListView.ContextMenu>
                <ContextMenu x:Name="contextMenu">
                    <MenuItem Name="btnAddEmp" Header="Добавить сотрудника"
Click="btnAddEmp_Click" />
                   
                </ContextMenu>
            </ListView.ContextMenu>
        </ListView>
        <Button x:Name="btnEmp" Grid.Row="3" Grid.Column="0" Content="Сотрудник"
Visibility="Collapsed" HorizontalAlignment="Right" Width="200" Margin="20 0 0 0"
Click="btnEmp_Click" />
        <Button x:Name="btnAddNewEmp" Grid.Row="3"
Content="Добавить сотрудника" HorizontalAlignment="Left" Width="200" Margin="8,24,0,23" Click="btnAddNewEmp_Click" Grid.Column="1" />

    </Grid>


</Page>
