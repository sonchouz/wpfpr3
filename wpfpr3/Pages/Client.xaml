<Page x:Class="wpfpr3.Pages.Client"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:wpfpr3.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Client" Background="White">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="220"/>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="80"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition Height="56"/>
            <RowDefinition/>
            <RowDefinition Height="60"/>
        </Grid.RowDefinitions>

        <!-- Поиск / фильтры -->
        <TextBox Name="txtSearch"
                 Grid.Row="1"
                 Grid.Column="0"
                 VerticalAlignment="Center"
                 Margin="5,0,5,0"/>

        <ComboBox Name="cmbSorting"
                  Grid.Row="1"
                  Grid.Column="1"
                  SelectedIndex="0"
                  VerticalAlignment="Center"
                  Margin="5,0,5,0"/>

        <ComboBox Name="cmbFilter"
                  Grid.Row="1"
                  Grid.Column="2"
                  SelectedIndex="0"
                  VerticalAlignment="Center"
                  Margin="5,0,5,0"/>

        <!-- Список кандидатов -->
        <ListView Name="lviewCand"
                  Grid.Column="0"
                  Grid.ColumnSpan="4"
                  Grid.Row="2"
                  Margin="5"
                  d:ItemsSource="{d:SampleData ItemCount=1}">

            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                </Style>
            </ListView.ItemContainerStyle>

            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border BorderBrush="Black"
                            BorderThickness="1"
                            Margin="2"
                            Padding="5">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="200"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="100"/>
                            </Grid.ColumnDefinitions>

                            <!-- Фото -->
                            <Image Width="180"
                                   Height="100"
                                   Margin="5"
                                   Grid.Column="0"
                                   Stretch="UniformToFill"
                                   Source="{Binding ImgPath,
                                                    FallbackValue=/wpfpr3;component/Resources/сотрудник.png}"/>

                            <!-- Текстовая информация -->
                            <StackPanel Grid.Column="1" Margin="5,0,0,0">
                                <TextBlock Text="{Binding User.surname}"/>
                                <TextBlock Text="{Binding User.firstname}"/>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="{Binding User.birthday}" Margin="0,0,10,0"/>
                                    <TextBlock Text="{Binding User.email}"/>
                                </StackPanel>
                            </StackPanel>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
